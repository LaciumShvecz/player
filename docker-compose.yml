version: '3.8'
services:

  # Environment
  mysql:
    image: mysql:8.0
    ports:
      - 3306:3306
    environment:
      MYSQL_ROOT_PASSWORD: admin
      MYSQL_DATABASE: player
      MYSQL_USER: admin
      MYSQL_PASSWORD: admin
    volumes:
      - ./media:/media


